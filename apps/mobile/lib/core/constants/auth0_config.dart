class Auth0Config {
  static const domain = 'YOUR_TENANT.auth0.com';
  static const clientId = 'YOUR_CLIENT_ID';

  // Callback URL registered in the Auth0 dashboard:
  // Android: https://{domain}/android/com.phoenix.mobile/callback
  // iOS:     https://{domain}/ios/com.phoenix.mobile/callback
}
